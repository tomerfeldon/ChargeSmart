# ChargeSmart backend dependencies

# Web / API
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# Auth
pyjwt==2.10.1

# Data tier — PostgreSQL via Supabase (M2 live wiring) + .env loading
psycopg[binary]==3.2.3
python-dotenv==1.0.1

# AI assistant (M6)
anthropic==0.42.0

# Tests
pytest==8.3.4
httpx==0.28.1
